main: "${project.groupId}.${project.name}"
name: "${project.name}"
version: "${project.version}"
website: "${project.url}"
api-version: "1.14"
authors:
  - JustEli
softdepend:
  - Vault
  - Treasury
  - WorldGuard
  - mcMMO
  - MythicMobs
commands:
  coins:
    aliases:
      - coin
  withdraw: {}
